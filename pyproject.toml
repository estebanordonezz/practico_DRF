[project]
name = "consecionaria"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "estebanordonezz", email = "e.ordonez@itecriocuarto.org.ar" }
]
requires-python = ">=3.12"
dependencies = [
    "django>=6.1",
    "djangorestframework>=3.18.0",
]

[project.scripts]
consecionaria = "consecionaria:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
